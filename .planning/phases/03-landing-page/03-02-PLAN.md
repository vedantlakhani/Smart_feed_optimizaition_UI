---
phase: 03-landing-page
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [LAND-01, LAND-02, LAND-03, LAND-04]

must_haves:
  truths:
    - "The hero section is visible above the fold on a 1280×768 screen without scrolling"
    - "The 3-step explainer can be read in under 30 seconds with zero jargon"
    - "The climate impact section shows a concrete CO₂ figure with a visible disclaimer"
    - "Clicking the CTA navigates to /dashboard and the demo_3stream.json scenario auto-loads"
  artifacts:
    - path: "dashboard/src/app/(marketing)/page.tsx"
      provides: "Verified landing page — no regressions introduced"
  key_links:
    - from: "human visual check at /"
      to: "/dashboard on CTA click"
      via: "browser navigation"
      pattern: "manual"
---

<objective>
Human visual verification of the completed landing page. Confirms above-fold layout, jargon-free copy, climate numbers credibility, and CTA navigation to a pre-loaded dashboard.

Purpose: Build output proves TypeScript correctness; only a human can confirm the hero fits above the fold, the 3-step copy reads cleanly, and the CTA flow works end-to-end in a real browser.
Output: Signed-off landing page ready for Phase 4 (Chemistry Validation) to proceed.
</objective>

<execution_context>
@/Users/vedantlakhani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedantlakhani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-landing-page/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm build is green</name>
  <files>dashboard/src/app/(marketing)/page.tsx</files>
  <read_first>
    - .planning/phases/03-landing-page/03-01-SUMMARY.md (confirm Plan 01 completed successfully, note any open issues)
  </read_first>
  <action>
Run the following two commands and confirm both exit 0:

```bash
cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run build 2>&1 | tail -10
cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run lint 2>&1 | tail -5
```

If build fails, diagnose and fix before proceeding to the checkpoint. Common fixes needed:
- TypeScript error on `motion.div` custom prop → ensure `variants={fadeUp}` is present alongside `custom={N}` on every motion element
- ESLint `no-unused-vars` → check that all imports (Droplets, etc.) are actually used in JSX; remove any that are not used
- `AnimatedGradientText` className prop type error → it accepts `string | undefined`; ensure no non-string is passed
- Missing import → check that all components (ShimmerButton, NumberTicker, BorderBeam, AnimatedGradientText, Card, CardContent) are imported with their exact paths from `@/components/magicui/` and `@/components/ui/card`

After both pass, start the dev server in the background:
```bash
cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run dev &
```
  </action>
  <verify>
    <automated>cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run build 2>&1 | grep -E "error TS|ESLint|✓ Compiled" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (output contains "Route (app)" table or "✓ Compiled", no "error TS" lines)
    - `npm run lint` exits 0 with no error output
    - `lsof -ti:3000` returns a PID (dev server running)
  </acceptance_criteria>
  <done>Build green, lint green, dev server running on port 3000</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of landing page</name>
  <files>dashboard/src/app/(marketing)/page.tsx</files>
  <action>
Human verification of the landing page at http://localhost:3000. Executor presents the verification checklist and waits for human approval signal. If any check fails, executor fixes the issue, re-runs the build, and re-presents the checkpoint.
  </action>
  <verify>
    <automated>grep "href.*dashboard" /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard/src/app/\(marketing\)/page.tsx | wc -l</automated>
  </verify>
  <what-built>
A complete marketing landing page at http://localhost:3000 with four sections:
1. Hero — problem statement ("Processing waste streams one at a time is expensive") + AnimatedGradientText badge + two ShimmerButton CTAs linking to /dashboard
2. How It Works — exactly 3 step cards on a slate-50 background (Load inventory / Optimizer finds blend / Get a plan)
3. Climate Impact — NumberTicker card counting up to "40 kg CO₂" with BorderBeam accent + disclaimer text
4. Final CTA — dark (slate-900) section with orange ShimmerButton "Open the Demo"

AnimatedGradientText gradient was patched from amber-blue to Ax-Orange (#FF8C00) / Ax-Cyan (#06B6D4).
  </what-built>
  <how-to-verify>
Open a browser window sized to 1280×768 (or use Chrome DevTools responsive mode at that size).

**Check 1 — Hero above the fold (LAND-01):**
Visit http://localhost:3000
Confirm WITHOUT scrolling: the eyebrow badge, H1 ("Processing waste streams one at a time is expensive"), the sub-paragraph about "supplemental fuel / neutralising chemicals", and at least one CTA button are all visible.
Expected: entire hero content visible without scrolling on a 1280×768 viewport.

**Check 2 — Jargon check (LAND-02):**
Read the 3-step section (scroll down to "How it works").
Confirm: NONE of these words appear anywhere on the page — r_water, BTU, Gatekeeper, B&B, blend ratio, SCWO, supercritical, NaOH, r_diesel.
Confirm: All 3 step cards are visible (Step 01, 02, 03) and the copy is plain English.
Expected: Zero jargon. Whole section readable in under 30 seconds.

**Check 3 — Climate impact section (LAND-03):**
Scroll down to the "Fewer emissions, not just lower costs" section.
Confirm: A card with a count-up animation appears (NumberTicker counting up to ~40 kg CO₂).
Confirm: A disclaimer sentence is present (look for "Estimated from representative 3-stream scenario").
Confirm: The "equivalent to taking a car off the road for ~330 km" comparison line is present.
Expected: Climate section looks credible and shows a disclaimer.

**Check 4 — CTA navigation (LAND-04):**
Click either "See the Optimizer in Action" (hero) or "Open the Demo" (bottom CTA).
Expected: Browser navigates to http://localhost:3000/dashboard.
On the dashboard, confirm: the file picker in the topbar shows "demo_3stream.json" selected (or 3 waste streams are visible in the Waste Streams tab without any user action).
Expected: Zero configuration required — demo data is auto-loaded.

**Check 5 — Gradient badge color:**
Look at the AnimatedGradientText eyebrow badge at the top of the hero.
Expected: Gradient cycles between orange and cyan (Ax brand colors), NOT amber and grey-blue.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 5 checks pass.
If any check fails, describe the issue (e.g., "hero doesn't fit above fold", "step 2 has jargon word X", "climate card missing disclaimer") and the executor will fix and re-present this checkpoint.
  </resume-signal>
  <done>Human has typed "approved" confirming all 5 visual checks passed</done>
</task>

</tasks>

<verification>
After human approval:

```bash
# Final confirmation — build still green after any fixes made during verification
cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run build 2>&1 | tail -3
```

Must exit 0.
</verification>

<success_criteria>
- Human approved all 5 visual checks
- Hero fits above the fold at 1280×768
- 3-step explainer has exactly 3 steps, zero jargon
- Climate section shows NumberTicker + disclaimer
- CTA navigates to /dashboard with demo data auto-loaded
- Build green after any fixes
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-page/03-02-SUMMARY.md` with:
- Human verification sign-off (approved or issues found and fixed)
- Any fixes applied during verification
- Final state of both modified files
- Phase 3 completion status: COMPLETE
</output>
