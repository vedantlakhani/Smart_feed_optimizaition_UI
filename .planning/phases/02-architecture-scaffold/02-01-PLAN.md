---
phase: 02-architecture-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/globals.css
  - dashboard/src/components/dashboard/topbar.tsx
  - dashboard/src/components/dashboard/impact-header.tsx
  - dashboard/src/components/dashboard/intro-tab.tsx
  - dashboard/src/components/dashboard/manifest-tab.tsx
  - dashboard/src/components/dashboard/operation-tab.tsx
  - dashboard/src/components/dashboard/cost-story.tsx
  - dashboard/src/components/dashboard/recipe-tab.tsx
autonomous: true
requirements:
  - PLSH-01
  - PLSH-02

must_haves:
  truths:
    - "Running `npm run build` inside dashboard/ exits 0 with no TypeScript errors"
    - "Zero instances of text-[#06b6d4], text-[#ff8c00], bg-[#06b6d4], bg-[#ff8c00], border-[#06b6d4], border-[#ff8c00] appear in className attributes across the 7 component files"
    - "globals.css @theme inline block contains --shadow-card, --shadow-card-hover, --ease-smooth, and --ease-spring"
    - "The Tailwind utilities text-ax-cyan, text-ax-orange, bg-ax-cyan, bg-ax-orange are used in components where hex was removed"
  artifacts:
    - path: "dashboard/src/app/globals.css"
      provides: "Design tokens: shadow elevation + easing curves in @theme inline block"
      contains: "--shadow-card"
    - path: "dashboard/src/components/dashboard/topbar.tsx"
      provides: "Brand color via Tailwind utility (text-ax-cyan)"
    - path: "dashboard/src/components/dashboard/impact-header.tsx"
      provides: "Brand color via Tailwind utilities"
    - path: "dashboard/src/components/dashboard/intro-tab.tsx"
      provides: "Brand color via Tailwind utilities"
    - path: "dashboard/src/components/dashboard/manifest-tab.tsx"
      provides: "Brand color via Tailwind utilities"
    - path: "dashboard/src/components/dashboard/operation-tab.tsx"
      provides: "Brand color via Tailwind utilities"
    - path: "dashboard/src/components/dashboard/cost-story.tsx"
      provides: "Brand color via Tailwind utilities"
    - path: "dashboard/src/components/dashboard/recipe-tab.tsx"
      provides: "Brand color via Tailwind utilities"
  key_links:
    - from: "dashboard/src/app/globals.css"
      to: "dashboard/src/components/dashboard/*.tsx"
      via: "@theme inline registers --color-ax-cyan as Tailwind utility text-ax-cyan"
      pattern: "text-ax-cyan|bg-ax-cyan|text-ax-orange|bg-ax-orange"
---

<objective>
Add Apple-like elevation and easing design tokens to globals.css, then replace every hardcoded hex className in the 7 dashboard component files with the equivalent Tailwind utility.

Purpose: PLSH-01 establishes the shadow/easing vocabulary all subsequent UI work will reference. PLSH-02 makes the design system single-source-of-truth so future brand color changes require editing one file.
Output: Updated globals.css with new @theme inline tokens; 7 component files with ax-cyan/ax-orange utilities replacing hardcoded hex; build green.
</objective>

<execution_context>
@/Users/vedantlakhani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedantlakhani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-scaffold/02-RESEARCH.md

<interfaces>
<!-- Key facts extracted from codebase — executor uses these directly, no exploration needed. -->

From dashboard/src/app/globals.css (lines 8-53):
The existing @theme inline block already contains:
```css
@theme inline {
  /* ... shadcn tokens ... */

  /* Custom Ax-brand colors — ALREADY PRESENT, do not duplicate */
  --color-ax-cyan: #06b6d4;
  --color-ax-cyan-light: #ecfeff;
  --color-ax-orange: #ff8c00;
  --color-ax-orange-light: #fff7ed;
}
```
Task 1 appends new tokens to this same block. Do NOT create a second @theme inline block.

From 02-RESEARCH.md — Hardcoded hex audit (7 files, className-only replacements):

| File | #06b6d4 className instances | #ff8c00 className instances |
|------|-----------------------------|-----------------------------|
| topbar.tsx | 2 | 0 |
| impact-header.tsx | 2 | 2 |
| intro-tab.tsx | 4 | 7 |
| manifest-tab.tsx | 2 | 2 |
| operation-tab.tsx | 5 | 6 |
| cost-story.tsx | 4 | 2 |
| recipe-tab.tsx | 2 | 2 |

Replacement map (className strings only):
- text-[#06b6d4] or text-[#06B6D4] → text-ax-cyan
- bg-[#06b6d4] or bg-[#06B6D4] → bg-ax-cyan
- border-[#06b6d4] or border-[#06B6D4] → border-ax-cyan
- text-[#ff8c00] or text-[#FF8C00] → text-ax-orange
- bg-[#ff8c00] or bg-[#FF8C00] → bg-ax-orange
- border-[#ff8c00] or border-[#FF8C00] → border-ax-orange

EXCLUDED — keep hex as-is (do not touch):
- const STREAM_COLORS = ["#ff8c00", "#06b6d4", ...] — Recharts fill props require literal CSS color strings
- <BorderBeam colorFrom="#ff8c00" colorTo="#06b6d4"> — magicui animation prop
- <ShimmerButton shimmerColor="#ff8c00"> — magicui animation prop
- Any hex in src/components/magicui/ — out of scope
- Any hex inside globals.css CSS utility classes — out of scope

From topbar.tsx (lines 38, 44) — current hardcoded hex:
```tsx
<Zap className="w-4 h-4 text-[#06b6d4]" />
<span className="text-[#06b6d4] font-bold text-lg tracking-tight leading-none">
```
After fix:
```tsx
<Zap className="w-4 h-4 text-ax-cyan" />
<span className="text-ax-cyan font-bold text-lg tracking-tight leading-none">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add elevation and easing design tokens to globals.css (PLSH-01)</name>
  <files>dashboard/src/app/globals.css</files>
  <read_first>
    - dashboard/src/app/globals.css (read the entire file to locate the @theme inline block boundary before editing)
  </read_first>
  <action>
Read globals.css. Find the closing `}` of the existing `@theme inline { ... }` block (currently ends after `--color-ax-orange-light: #fff7ed;`). Insert the following tokens immediately before that closing brace — do NOT create a second @theme inline block:

```css
  /* Elevation shadows (Apple-like card depth) */
  --shadow-card: 0 1px 3px 0 rgb(0 0 0 / 0.07), 0 1px 2px -1px rgb(0 0 0 / 0.07);
  --shadow-card-hover: 0 4px 12px 0 rgb(0 0 0 / 0.10), 0 2px 6px -2px rgb(0 0 0 / 0.08);
  --shadow-topbar: 0 1px 0 0 #e2e8f0;

  /* Smooth cubic-bezier easing */
  --ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);

  /* Whitespace scale */
  --spacing-section: 5rem;
  --spacing-card: 1.5rem;
```

These tokens generate the Tailwind utilities: `shadow-card`, `shadow-card-hover`, `shadow-topbar`, `ease-smooth`, `ease-spring`, `ease-out-expo` — available to all subsequent phases.
  </action>
  <verify>
    <automated>grep -c "shadow-card\|ease-smooth\|ease-spring" /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep returns a count of at least 3 (shadow-card, ease-smooth, ease-spring all present)
    - There is exactly ONE @theme inline block in globals.css (grep "@theme inline" returns 1 line)
    - The existing --color-ax-cyan and --color-ax-orange entries are unchanged
  </acceptance_criteria>
  <done>globals.css @theme inline block contains all six new tokens (shadow-card, shadow-card-hover, shadow-topbar, ease-smooth, ease-spring, ease-out-expo) within the single existing @theme inline block.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex with Tailwind utilities across 7 component files (PLSH-02)</name>
  <files>
    dashboard/src/components/dashboard/topbar.tsx,
    dashboard/src/components/dashboard/impact-header.tsx,
    dashboard/src/components/dashboard/intro-tab.tsx,
    dashboard/src/components/dashboard/manifest-tab.tsx,
    dashboard/src/components/dashboard/operation-tab.tsx,
    dashboard/src/components/dashboard/cost-story.tsx,
    dashboard/src/components/dashboard/recipe-tab.tsx
  </files>
  <read_first>
    - dashboard/src/components/dashboard/topbar.tsx
    - dashboard/src/components/dashboard/impact-header.tsx
    - dashboard/src/components/dashboard/intro-tab.tsx
    - dashboard/src/components/dashboard/manifest-tab.tsx
    - dashboard/src/components/dashboard/operation-tab.tsx
    - dashboard/src/components/dashboard/cost-story.tsx
    - dashboard/src/components/dashboard/recipe-tab.tsx
  </read_first>
  <action>
Read each of the 7 files. For every occurrence of the hardcoded hex values in className strings, apply the following replacements:

Cyan replacements (in className attributes only):
- text-[#06b6d4] → text-ax-cyan
- text-[#06B6D4] → text-ax-cyan
- bg-[#06b6d4] → bg-ax-cyan
- bg-[#06B6D4] → bg-ax-cyan
- border-[#06b6d4] → border-ax-cyan
- border-[#06B6D4] → border-ax-cyan
- ring-[#06b6d4] → ring-ax-cyan
- fill-[#06b6d4] → fill-ax-cyan (only in className, not Recharts fill prop)

Orange replacements (in className attributes only):
- text-[#ff8c00] → text-ax-orange
- text-[#FF8C00] → text-ax-orange
- bg-[#ff8c00] → bg-ax-orange
- bg-[#FF8C00] → bg-ax-orange
- border-[#ff8c00] → border-ax-orange
- border-[#FF8C00] → border-ax-orange

DO NOT touch:
- Any line containing `STREAM_COLORS` (JS array for Recharts)
- Any line containing `colorFrom=`, `colorTo=`, `shimmerColor=` (magicui props)
- Any file inside `src/components/magicui/`
- Any hex value that is NOT inside a className string

After replacing, verify that the file is syntactically unchanged aside from the substitutions — no lines should be added or removed, only hex → utility name substitutions within existing className attributes.
  </action>
  <verify>
    <automated>grep -rn "text-\[#06b6d4\]\|text-\[#ff8c00\]\|text-\[#06B6D4\]\|text-\[#FF8C00\]\|bg-\[#06b6d4\]\|bg-\[#ff8c00\]\|border-\[#06b6d4\]\|border-\[#ff8c00\]" /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard/src/components/dashboard/ --include="*.tsx" 2>/dev/null | grep -v "magicui" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep command returns 0 — no hardcoded hex className patterns remain in any of the 7 component files
    - Each replaced className still functions identically (text-ax-cyan produces the same #06b6d4 color as before via the registered @theme inline token)
    - STREAM_COLORS hex arrays in recipe-tab.tsx and operation-tab.tsx are unchanged
    - magicui component props (colorFrom, colorTo, shimmerColor) are unchanged
    - `cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>All 7 component files use text-ax-cyan / text-ax-orange / bg-ax-cyan / bg-ax-orange / border-ax-cyan / border-ax-orange Tailwind utilities in place of hardcoded hex. npm run build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks in order:

1. Token presence:
   `grep -c "shadow-card\|ease-smooth\|ease-spring\|ease-out-expo" dashboard/src/app/globals.css`
   Expected: 4 or more

2. No hardcoded hex in component classNames:
   `grep -rn "#06b6d4\|#ff8c00\|#06B6D4\|#FF8C00" dashboard/src/components/dashboard/ --include="*.tsx" | grep "className" | grep -v "magicui"`
   Expected: 0 lines

3. Build integrity:
   `cd /Users/vedantlakhani/Desktop/AxNano/Smart_feed_optimizaition/dashboard && npm run build 2>&1 | tail -5`
   Expected: exits 0, "Compiled successfully" or equivalent
</verification>

<success_criteria>
1. globals.css @theme inline block contains all 6 new tokens (--shadow-card, --shadow-card-hover, --shadow-topbar, --ease-smooth, --ease-spring, --ease-out-expo)
2. Zero hardcoded hex values in className strings across all 7 dashboard component files
3. npm run build exits 0 (TypeScript clean, no route conflicts)
4. Existing Recharts STREAM_COLORS arrays and magicui animation props retain their literal hex values
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-scaffold/02-01-SUMMARY.md` following the standard summary template.
</output>
